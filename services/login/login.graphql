type AuthLogin {
    email: String!
    token: String
    path: String
}

extend type Query {
    login: [AuthLogin]
}

extend type Mutation {
    "Mutacion para login de usuario"
    loginIn(email: String!, password: String!) : AuthLogin
}