type User{
    _id: ID!,
    name: String!
    username: String!
    email: String!
    password: String!
    avatar: String
}

type Query {
    login: [User]
}

type Mutation {
    loginIn(email: String!, password: String!) : User
}