type User{
    _id: ID!,
    name: String!
    username: String!
    email: String!
    password: String!
    avatar: String
}

extend type Query {
    login: [User]
}

extend type Mutation {
    loginIn(email: String!, password: String!) : User
}