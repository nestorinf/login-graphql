type UserLogin{
    _id: ID!,
    name: String!
    username: String!
    email: String!
    avatar: String
}

extend type Query {
    login: [UserLogin]
}

extend type Mutation {
    loginIn(email: String!, password: String!) : UserLogin
}